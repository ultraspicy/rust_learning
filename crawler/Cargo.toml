[package]
name = "crawler"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
tokio-postgres = "0.7"
rand = "0.9.2"
dotenv = "0.15"
reqwest = { version = "0.11", features = ["gzip", "brotli", "deflate"] }
scraper = "0.17"
